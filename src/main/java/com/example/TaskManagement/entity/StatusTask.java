package com.example.TaskManagement.entity;

/**
 * Represents the status of a task.
 * This enum is used to define the possible states a task can be in throughout its lifecycle.
 */
public enum StatusTask {
    WAITING,
    IN_PROCESS,
    COMPLETED
}
