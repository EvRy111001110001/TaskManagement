package com.example.TaskManagement.entity;

/**
 * Enum representing the priority levels of a task.
 * This enum is used to classify tasks based on their importance or urgency.
 */
public enum PriorityTask {
    HIGH,
    MEDIUM,
    LOW
}
